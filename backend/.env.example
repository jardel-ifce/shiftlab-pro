# =============================================================================
# ShiftLab Pro - Variáveis de Ambiente
# =============================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha os valores conforme seu ambiente
# 3. NUNCA commite o arquivo .env no git (já está no .gitignore)
# =============================================================================

# =============================================================================
# AMBIENTE
# =============================================================================
# Ambiente de execução: development | staging | production
ENVIRONMENT=development

# Modo debug (True apenas em desenvolvimento)
DEBUG=True

# =============================================================================
# APLICAÇÃO
# =============================================================================
# Nome da aplicação
APP_NAME="ShiftLab Pro"

# Versão da API
API_VERSION=v1

# Prefixo das rotas da API
API_PREFIX=/api/v1

# Host e porta do servidor
HOST=0.0.0.0
PORT=8000

# Domínios permitidos para CORS (separados por vírgula)
# Desenvolvimento: http://localhost:3000,http://localhost:5173
# Produção: https://seudominio.com.br
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173

# =============================================================================
# BANCO DE DADOS
# =============================================================================
# Formato da URL de conexão:
# - SQLite (dev):     sqlite+aiosqlite:///./shiftlab.db
# - PostgreSQL (prod): postgresql+asyncpg://usuario:senha@host:porta/database

# Desenvolvimento (SQLite - arquivo local)
DATABASE_URL=sqlite+aiosqlite:///./shiftlab.db

# Produção (PostgreSQL) - descomente e configure:
# DATABASE_URL=postgresql+asyncpg://shiftlab_user:sua_senha_segura@localhost:5432/shiftlab_db

# Configurações adicionais do banco
DATABASE_ECHO=False
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# =============================================================================
# AUTENTICAÇÃO JWT
# =============================================================================
# IMPORTANTE: Gere uma chave secreta forte para produção!
# Comando para gerar: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=sua-chave-secreta-muito-forte-troque-em-producao-123456789

# Algoritmo de criptografia do JWT
JWT_ALGORITHM=HS256

# Tempo de expiração do access token (em minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Tempo de expiração do refresh token (em dias)
REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# SEGURANÇA
# =============================================================================
# Custo do bcrypt (quanto maior, mais seguro, porém mais lento)
# Recomendado: 12 para produção, 4 para testes
BCRYPT_ROUNDS=12

# Número máximo de tentativas de login antes de bloquear
MAX_LOGIN_ATTEMPTS=5

# Tempo de bloqueio após exceder tentativas (em minutos)
LOGIN_LOCKOUT_MINUTES=15

# =============================================================================
# PRIMEIRO USUÁRIO ADMIN
# =============================================================================
# Dados do primeiro usuário administrador (criado automaticamente)
FIRST_ADMIN_EMAIL=admin@shiftlab.com.br
FIRST_ADMIN_PASSWORD=Admin@123456
FIRST_ADMIN_NAME=Administrador

# =============================================================================
# EMAIL (para notificações e alertas)
# =============================================================================
# Servidor SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app
SMTP_FROM_EMAIL=noreply@shiftlab.com.br
SMTP_FROM_NAME="ShiftLab Pro"

# Habilitar envio de emails
SMTP_ENABLED=False

# =============================================================================
# LOGS
# =============================================================================
# Nível de log: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# Formato do log: json | text
LOG_FORMAT=text

# Caminho do arquivo de log (deixe vazio para stdout)
LOG_FILE=

# =============================================================================
# CACHE (Redis - opcional)
# =============================================================================
# URL do Redis para cache (deixe vazio para desabilitar)
REDIS_URL=

# Tempo padrão de expiração do cache (em segundos)
CACHE_TTL=300

# =============================================================================
# CONFIGURAÇÕES DE NEGÓCIO
# =============================================================================
# Intervalo padrão entre trocas de óleo (em km)
DEFAULT_OIL_CHANGE_KM=10000

# Intervalo padrão entre trocas de óleo (em meses)
DEFAULT_OIL_CHANGE_MONTHS=6

# Margem de alerta antes da próxima troca (em km)
ALERT_MARGIN_KM=500

# Margem de alerta antes da próxima troca (em dias)
ALERT_MARGIN_DAYS=15

# =============================================================================
# STORAGE (para uploads de arquivos)
# =============================================================================
# Tipo de storage: local | s3
STORAGE_TYPE=local

# Diretório para uploads locais
UPLOAD_DIR=./uploads

# Tamanho máximo de upload (em MB)
MAX_UPLOAD_SIZE_MB=10

# Tipos de arquivo permitidos (separados por vírgula)
ALLOWED_EXTENSIONS=pdf,jpg,jpeg,png

# =============================================================================
# PAGINAÇÃO
# =============================================================================
# Número padrão de itens por página
DEFAULT_PAGE_SIZE=20

# Número máximo de itens por página
MAX_PAGE_SIZE=100
